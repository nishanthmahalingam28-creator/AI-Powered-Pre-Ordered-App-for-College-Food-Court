document.addEventListener('DOMContentLoaded',loadMorningPoll);
const API_BASE=window.FOOD_COURT_API_BASE||(typeof window.getApiUrl==='function'?window.getApiUrl(''):'/api');
async function loadMorningPoll(){try{const auth=await fetch(API_BASE+'/auth/me',{credentials:'include'}),ad=await auth.json();if(!auth.ok||!ad.authenticated||ad.user.role!=='customer'){location.href='../auth/login.html';return}const res=await fetch(API_BASE+'/customer/morning-poll/today',{credentials:'include'}),data=await res.json();if(!res.ok||!data.success)throw Error(data.message||'Unable to load morning survey.');document.getElementById('poll-loading').classList.add('hidden');const list=document.getElementById('poll-list'),surveys=data.surveys||[];if(!surveys.length){document.getElementById('poll-empty').classList.remove('hidden');return}list.innerHTML=surveys.map(renderSurvey).join('')}catch(e){document.getElementById('poll-loading').classList.add('hidden');showPollMessage(e.message||'Unable to load morning survey.',false)}}
function renderSurvey(s){return `<section class="bg-white rounded-3xl border border-slate-200 shadow-sm p-5 sm:p-6"><div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 mb-5"><div><p class="text-[10px] font-black uppercase tracking-widest text-teal-600">Morning Food Check</p><h2 class="text-xl font-black text-slate-900 mt-1">${escapeHtml(s.shop_name||'Food Court')}</h2><p class="text-xs text-slate-400 mt-1">Do you need food today?</p></div><span class="px-3 py-1.5 rounded-full text-[10px] font-black ${s.voted?'bg-emerald-100 text-emerald-700':'bg-amber-100 text-amber-700'}">${s.voted?'✓ Response Recorded':'Response Needed'}</span></div><div class="grid grid-cols-1 sm:grid-cols-2 gap-4"><label class="cursor-pointer rounded-2xl border-2 border-slate-200 hover:border-emerald-400 p-5 text-center"><input type="radio" name="need-food-${s.survey_id}" value="yes" class="sr-only"><i class="fa-solid fa-utensils text-2xl text-emerald-600"></i><p class="font-black mt-2">Yes, I need food</p><p class="text-xs text-slate-400 mt-1">I plan to eat today.</p></label><label class="cursor-pointer rounded-2xl border-2 border-slate-200 hover:border-slate-400 p-5 text-center"><input type="radio" name="need-food-${s.survey_id}" value="no" class="sr-only"><i class="fa-solid fa-ban text-2xl text-slate-500"></i><p class="font-black mt-2">No, I don't need food</p><p class="text-xs text-slate-400 mt-1">I don't plan to eat today.</p></label></div><div class="mt-5 flex justify-end"><button onclick="submitFoodNeed(${s.survey_id})" class="px-5 py-3 rounded-xl bg-teal-600 hover:bg-teal-700 text-white text-xs font-black ${s.voted?'hidden':''}">Submit Response</button></div></section>`}
async function submitFoodNeed(id){const selected=document.querySelector(`input[name="need-food-${id}"]:checked`);if(!selected){showPollMessage('Please choose Yes or No.',false);return}try{const res=await fetch(API_BASE+'/customer/morning-poll/vote',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({survey_id:id,need_food:selected.value==='yes'})}),data=await res.json();if(!res.ok||!data.success)throw Error(data.message||'Unable to record response.');showPollMessage('Your morning food response has been recorded.',true);await loadMorningPoll()}catch(e){showPollMessage(e.message||'Unable to record response.',false)}}
function showPollMessage(m,ok){const e=document.getElementById('poll-message');e.textContent=m;e.className='mb-5 p-4 rounded-2xl text-sm font-bold '+(ok?'bg-emerald-50 border border-emerald-200 text-emerald-700':'bg-rose-50 border border-rose-200 text-rose-700');e.classList.remove('hidden')}
function escapeHtml(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}